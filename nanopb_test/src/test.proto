// A very simple protocol definition, consisting of only
// one message.

syntax = "proto3";

import "nanopb.proto";

message SimpleMessage {
    int32 sender = 1;
    string buffer = 2 [(nanopb).initializer = "\"AA\""];;
    int32 receiver = 3;
}

message Envelope {
    uint32 address = 1;
    repeated SimpleMessage letter = 2;
} 