// A very simple protocol definition, consisting of only
// one message.

syntax = "proto3";

message SimpleMessage {
    int32 sender = 1;
    string buffer = 2 [(nanopb).initializer = "00"];
    int32 receiver = 3;
}

message Envelope {
    uint32 address = 1;
    repeated SimpleMessage letter = 2;
} 